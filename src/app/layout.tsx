import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Gandalf Challenge - Test Your Prompt Engineering Skills",
  description: "A gamified challenge to trick an AI into revealing secret passwords through prompt engineering. Learn about AI security vulnerabilities and defenses.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
